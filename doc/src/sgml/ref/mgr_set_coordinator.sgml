<!--
doc/src/sgml/ref/mgr_set_coordinator.sgml
ADB documentation
-->

<refentry id="MGR-SETCOORDINATOR">
  <refmeta>
    <refentrytitle>SET COORDINATOR</refentrytitle>
    <manvolnum>7</manvolnum>
    <refmiscinfo>Manage - Language Statements</refmiscinfo>
  </refmeta>

  <refnamediv>
    <refname>SET COORDINATOR</refname>
    <refpurpose>change the the value of parameter in the file of postgres.conf which is own to datanode</refpurpose>
  </refnamediv>

  <indexterm zone="mgr-setcoordinator">
    <primary>SET COORDINATOR</primary>
  </indexterm>

  <refsynopsisdiv>
    <synopsis>
SET COORDINATOR ALL { parameter = <replaceable class="parameter">value</replaceable> } [, ...] ) [ FORCE ];
SET COORDINATOR <replaceable class="parameter">nodename</replaceable> { parameter = <replaceable class="parameter">value</replaceable> } [, ...] ) [ FORCE ];
<phrase>where <replaceable class="parameter">parameter</replaceable> can be one of:</phrase>
    if you add "force" at the tail of the command, it won't check the the value and name of parameter, and it alse won't check the parameter whether exist
	in the file of postgres.conf.
    </synopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>Description</title>

    <para>
      change the the value of parameter in the file of postgres.conf and update PARA table, it won't check the the value and name of parameter correctly if adding 'force' at the tail of command line,in addition,it will update PARA table.
    </para>
  </refsect1>

  <refsect1>
    <title>Parameters</title>

    <variablelist>
      <varlistentry>
        <term>
          <replaceable class="parameter">Parameter</replaceable>
        </term>
        <listitem>
          <para>
            the parameter that in the file of postgres.conf
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsect1>


  <refsect1>
    <title>Examples</title>

    <para>
      change the the value of 'synchronous_commit' and 'max_wal_senders' in the file of postgres.conf:
      <programlisting>
        set coordinator coord1(synchronous_commit = off, max_wal_senders = 10)force;
      </programlisting>
    </para>
  </refsect1>

  <refsect1>
    <title>See Also</title>

    <simplelist type="inline">
      <member><xref linkend="mgr-resetcoordinator"></member>
    </simplelist>
  </refsect1>


</refentry>
