<!--
doc/src/sgml/ref/mgr_monitor_node.sgml
ADB documentation
-->

<refentry id="MGR-MONITORNODE">
  <refmeta>
    <refentrytitle>MONITOR</refentrytitle>
    <manvolnum>7</manvolnum>
    <refmiscinfo>Manage - Language Statements</refmiscinfo>
  </refmeta>

  <refnamediv>
    <refname>MONITOR</refname>
    <refpurpose>monitor the current state of the ADB cluster node</refpurpose>
  </refnamediv>

  <indexterm zone="mgr-monitornode">
    <primary>MONITOR</primary>
  </indexterm>

  <refsynopsisdiv>
    <synopsis>
MONITOR <replaceable class="parameter">option</replaceable> [ALL | <replaceable class="parameter">nodename</replaceable>[,...]];

<phrase>where <replaceable class="parameter">option</replaceable> can be one of:</phrase>
	GTM
	GTM MASTER
	GTM SLAVE
	GTM EXTRA
	COORDINATOR
	DATANODE
	DATANODE MASTER
	DATANODE SLAVE
	DATANODE EXTRA
    </synopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>Description</title>

    <para>
monitor the current state of the ADB cluster node.
    </para>
    <para>
node has three operating conditions:running, not running and reject connection.
    </para>

    <para>
running means is server is accepting connections;
    </para>

    <para>
not running means is could not establish connection;
    </para>

    <para>
reject connection means is server is alive but rejecting connections(e.g., recovery model or startup model).
    </para>
  </refsect1>
  <refsect1>
    <title>Parameters</title>

    <variablelist>
      <varlistentry>
        <term>
          <replaceable class="parameter">nodename</replaceable>
        </term>
        <listitem>
          <para>
            Node name, corresponds to the name column in the node table.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsect1>
  <refsect1>
    <title>Examples</title>

    <para>
      monitor the state for datanode master all:
      <programlisting>
        monitor datanode master all;
      </programlisting>
    </para>
  </refsect1>


</refentry>
